*.iml
.gradle
/local.properties
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
.DS_Store
/build
/captures
.externalNativeBuild
# Claude Code local dir (worktrees/state); a stale Windows-path worktree here
# broke `git status` until ignored — see PM ledger STD note.
.claude/
# IDE caches + agent runtime message inboxes (commit_hygiene: don't commit)
/.idea/caches/
.messages/
