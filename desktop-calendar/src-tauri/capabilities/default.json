{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的默认权限。界面要监听 Rust 侧派过来的 agent-request 事件，所以需要 core:event。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
